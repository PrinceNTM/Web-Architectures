{
  "name": "web-architectures",
  "version": "1.0.0",
  "description": "A full-stack habit tracking application built with React + Vite (frontend) and Express (backend).",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "setup:env": "node scripts/ensure-env.cjs",
    "preflight": "npm run setup:env && node scripts/preflight.cjs",
    "ports:clean:dev": "kill-port 3000 5173 || echo Dev ports were already free",
    "dev": "npm run preflight && npm run ports:clean:dev && concurrently -k -n backend,frontend -c green,blue \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cross-env PORT=3000 FRONTEND_URL=http://localhost:5173 npm --prefix backend run dev:full",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm run preflight && npm run test:all",
    "test:all": "npm run test:unit && npm run e2e:ci",
    "test:unit": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --prefix backend run test",
    "test:frontend": "npm --prefix frontend run test",
    "test:coverage": "npm run test:coverage:backend && npm run test:coverage:frontend",
    "test:coverage:backend": "npm --prefix backend run test:coverage",
    "test:coverage:frontend": "npm --prefix frontend run test:coverage",
    "ports:clean": "kill-port 3100 5174 || echo Test ports were already free",
    "start:backend:test": "cross-env PORT=3100 FRONTEND_URL=http://localhost:5174 npm --prefix backend run start:test",
    "start:frontend:test": "cross-env VITE_API_BASE_URL=http://localhost:3100/api npm --prefix frontend run dev -- --host 127.0.0.1 --port 5174",
    "e2e:run": "cypress run",
    "e2e:run:ci": "cypress run --config baseUrl=http://localhost:5174 --env apiUrl=http://localhost:3100/api",
    "e2e:report": "cypress run --reporter junit --reporter-options mochaFile=cypress/results/junit-[hash].xml",
    "e2e:ci": "npm run ports:clean && start-server-and-test \"npm run start:backend:test\" http://localhost:3100/api/health \"start-server-and-test \\\"npm run start:frontend:test\\\" http://localhost:5174 \\\"npm run e2e:run:ci\\\"\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PrinceNTM/Web-Architectures.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/PrinceNTM/Web-Architectures/issues"
  },
  "homepage": "https://github.com/PrinceNTM/Web-Architectures#readme",
  "devDependencies": {
    "concurrently": "^10.0.4",
    "cross-env": "^7.0.3",
    "cypress": "^15.19.0",
    "kill-port": "^2.0.1",
    "start-server-and-test": "^3.0.11",
    "typescript": "^5.2.2",
    "wait-on": "^9.1.0"
  }
}
